#!/bin/sh

# The generator filters relevant events and serializes against the fw4 include.
[ ! -x /usr/share/bridge-flow-offload/apply-rules.sh ] ||
	/usr/share/bridge-flow-offload/apply-rules.sh --iface-event
