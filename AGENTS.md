# W1700K 버전별 릴리스

사용자 지시: r 버전이 올라갈 때마다 GitHub에 소스와 빌드를 올리고, 개선 사항을 릴리스에 기록한다. 전체 조사 목표가 끝날 때까지 배포를 미루지 않는다.

- 배포 저장소는 `VultaV/OpenW1700k`, push remote는 `fork`다. 원본 `origin`에 배포하지 않는다.
- 각 r 버전의 빌드가 준비되면 해당 소스 커밋·태그를 push하고 GitHub Release에 펌웨어, `SHA256SUMS`, 빌드 검증 기록을 첨부한다. 기존 버전의 태그나 펌웨어를 다른 내용으로 덮어쓰지 않는다.
- 한국어 릴리스 노트는 `docs/releases/<tag>.md`에 보존하고 `gh release create/edit --notes-file`로 반영한다. 이전 버전 대비 변경 이유·개선 사항, 실제 검증 결과, 남은 문제, 설치·부팅 시험 여부와 커널/모듈 ABI를 명시한다.
- 실험 버전은 prerelease로 공개한다. 빌드 실패나 소스 후보만 있는 상태를 완성된 펌웨어로 표시하지 않는다. 다른 버전에서 측정한 결과에는 측정한 버전을 명시한다.
- 업로드 후 원격 커밋·태그, 릴리스 본문과 첨부 파일의 이름·크기·SHA256을 확인한다. 비밀번호, 설정 백업, Tailscale 상태, 단말 식별자, 원시 패킷은 공개하지 않는다.
- GitHub 릴리스 배포와 공유기 설치는 별개다. 설치·재부팅에는 세션에서 정한 유선 연결 보호 조건을 그대로 적용한다.
